name: 'test'

on:
  push:
  pull_request:

jobs:
  build:
    uses: codecentricnl/declaration-event-service/.github/workflows/build.yml@main

  test:
    uses: codecentricnl/declaration-event-service/.github/workflows/workflow-test-report.yml@main
    needs:
      - build
